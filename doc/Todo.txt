Users module Todo
-----------------

NIS:
- edit fields on client
    - problem: tools are interactive?

LDAP:
- edit password
    - problem: doesn't work with cpu

- adding users to LDAP groups
    - problem: not possible with cpu?

- dialog for definition query filter
    - problem: needed all id's, usernames for checking
    
- "reconnect to server" button (with new password/query)

- showing error cpu's messages

Kerberos:

- edit password 
    - problem: password changing utilities are interactive

Expert login dialog (authentication):

- better workflow (some checkboxes?)

- other values for pam/nsswitch (krb5 - required?)

- do not enable call from installation?

Dialogs behaviour:

- groups and users summary to one dialog

System users:

- script to gather all system users from rpms
