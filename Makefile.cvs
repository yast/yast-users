#
# Makefile.cvs for libycp
#
configure: all
	./configure

all:
	y2tool y2automake
	aclocal
	autoconf
	autoheader
	automake -a

install: configure
	make
	make install

reconf: all
	./config.status --recheck
	./config.status
